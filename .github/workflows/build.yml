name: Build Docker Images

on:
  push:
    branches: [main]
    paths:
      - 'server-nestjs/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:
    inputs:
      app:
        description: 'App to build'
        required: true
        type: choice
        options:
          - app-api
          - notification
          - cronjob-worker
          - all

env:
  REGISTRY: ${{ secrets.DOCKER_USERNAME }}

jobs:
  # Auto build all on push to main
  build-on-push:
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - app: app-api
            port: 3000
          - app: notification
            port: 3002
          - app: cronjob-worker
            port: 3003
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push ${{ matrix.app }}
        uses: docker/build-push-action@v5
        with:
          context: ./server-nestjs
          file: ./server-nestjs/Dockerfile
          push: true
          build-args: |
            APP_NAME=${{ matrix.app }}
            APP_PORT=${{ matrix.port }}
          tags: |
            ${{ env.REGISTRY }}/pm-${{ matrix.app }}:${{ github.sha }}
            ${{ env.REGISTRY }}/pm-${{ matrix.app }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        run: |
          echo "### Build ${{ matrix.app }} Successful! :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ env.REGISTRY }}/pm-${{ matrix.app }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  # Manual build single app
  build-single:
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.app != 'all' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set port based on app
        id: set-port
        run: |
          case "${{ github.event.inputs.app }}" in
            app-api) echo "port=3000" >> $GITHUB_OUTPUT ;;
            notification) echo "port=3002" >> $GITHUB_OUTPUT ;;
            cronjob-worker) echo "port=3003" >> $GITHUB_OUTPUT ;;
          esac

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./server-nestjs
          file: ./server-nestjs/Dockerfile
          push: true
          build-args: |
            APP_NAME=${{ github.event.inputs.app }}
            APP_PORT=${{ steps.set-port.outputs.port }}
          tags: |
            ${{ env.REGISTRY }}/pm-${{ github.event.inputs.app }}:${{ github.sha }}
            ${{ env.REGISTRY }}/pm-${{ github.event.inputs.app }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        run: |
          echo "### Build Successful! :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**App:** ${{ github.event.inputs.app }}" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ env.REGISTRY }}/pm-${{ github.event.inputs.app }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  # Manual build all apps
  build-all:
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.app == 'all' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - app: app-api
            port: 3000
          - app: notification
            port: 3002
          - app: cronjob-worker
            port: 3003
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push ${{ matrix.app }}
        uses: docker/build-push-action@v5
        with:
          context: ./server-nestjs
          file: ./server-nestjs/Dockerfile
          push: true
          build-args: |
            APP_NAME=${{ matrix.app }}
            APP_PORT=${{ matrix.port }}
          tags: |
            ${{ env.REGISTRY }}/pm-${{ matrix.app }}:${{ github.sha }}
            ${{ env.REGISTRY }}/pm-${{ matrix.app }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        run: |
          echo "### Build ${{ matrix.app }} Successful! :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ env.REGISTRY }}/pm-${{ matrix.app }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

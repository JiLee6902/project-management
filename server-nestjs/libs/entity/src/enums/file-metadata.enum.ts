export enum FileVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum FileStatus {
  PENDING = 'pending',
  UPLOADED = 'uploaded',
  FAILED = 'failed',
}

export enum FileType {
  IMAGE = 'image',
  DOCUMENT = 'document',
}
